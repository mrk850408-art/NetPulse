# NetPulse
an advanced local network scanner that discovers devices, identifies their manufacturer and friendly names (mDNS/SSDP), detects ARP spoofing, and optionally scans for open ports — all with color-coded output and SQLite storage.
